query getEnsRegistrations($address: ID!) {
    account(id: $address) {
        id
        registrations {
            domain {
                name
            }
            expiryDate
        }
    }
}
