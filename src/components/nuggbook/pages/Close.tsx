import React from 'react';

import { NuggBookPage } from '@src/interfaces/nuggbook';

const Close: NuggBookPage = () => {
    return (
        <div
            style={{
                justifyContent: 'flex-start',
                alignItems: 'center',
                display: 'flex',
                flexDirection: 'column',
                width: '100%',
                height: '100%',
            }}
        />
    );
};

export default Close;
